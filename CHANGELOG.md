# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [1.2.0] - 2024-11-11

### ✨ Added

- **智能错误处理**：新增 `normalizeError` 方法，自动处理各种类型的错误
  - 支持 Error 对象、字符串、普通对象等任意类型
  - 自动提取错误信息（支持 `message`、`errMsg`、`msg`、`error` 字段）
  - 智能序列化复杂对象，避免 `[object Object]` 显示问题

- **循环引用处理**：新增 `safeStringify` 方法
  - 安全地序列化对象为 JSON
  - 自动处理循环引用（标记为 `[Circular]`）
  - 防止序列化失败

- **新增辅助函数**：
  - `clearUserInfo()`：清除用户信息（用于退出登录场景）
  - `captureError(error, context)`：捕获带上下文信息的错误（推荐使用）

- **示例代码**：新增 `examples/error-handling.js`，包含 10 个实际使用场景

### 🔧 Improved

- 优化 `captureException` 方法，现在支持任意类型的错误参数
- 优化 `setupUnhandledRejectionHandler`，使用新的错误规范化逻辑
- 更新 README 文档，添加详细的错误处理说明和示例

### 📝 Documentation

- 添加错误处理最佳实践文档
- 更新 API 文档，补充 `clearUserInfo` 和 `captureError` 说明
- 添加多种使用场景的代码示例

## [1.1.1] - 2024-11-11

### 🔧 Changed

- 将 `sentry-mina` 从 `peerDependencies` 改为 `dependencies`
- 用户现在只需安装 `sentry-xcx` 一个包，无需单独安装 `sentry-mina`

### 📦 Build

- 更新 Rollup 配置，将 `sentry-mina` 打包到最终库中
- 包大小增加到约 110KB（包含 sentry-mina）

### 📝 Documentation

- 更新安装说明，移除 `sentry-mina` 的单独安装要求
- 美化 README，添加专业徽章和布局
- 添加详细的贡献指南

## [1.0.0] - 2024-11-10

### Added
- 🎉 初始版本发布
- ✨ 核心 Sentry 封装功能
- 📦 支持 TypeScript 类型定义
- 🎯 提供丰富的辅助函数
- 🔄 支持 App 和 Page 生命周期自动包装
- 🎭 支持装饰器自动捕获异常
- 📊 支持面包屑追踪
- 🔧 支持灵活的配置选项和钩子函数
- 📝 完整的文档和示例代码

